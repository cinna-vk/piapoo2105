init python:

    class Task:
        def __init__(self, title, priority, description, due_date, status):
            self.title = title
            self.priority = priority
            self.description = description
            self.due_date = due_date
            self.status = status